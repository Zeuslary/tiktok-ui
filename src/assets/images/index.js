const images = {
    logo: require('./logo.svg').default,
    logoIcon: require('./logo-icon.svg').default,
    zuri: require('./zuri.png'),
    noImg: require('./noImage.png'),
};

export default images;
